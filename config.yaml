# 非密钥配置（API Key 请放在 .env）
# 优先级：环境变量 > config.yaml > 代码默认值

paths:
  pdf: "（2026.V3）NCCN临床 实践指南：B细胞淋巴瘤.pdf"
  knowledge_base: data/processed/dlbcl_knowledge_base.json
  bm25_index: data/indexes/bm25_index.pkl
  vector_index: data/indexes/vector
  knowledge_graph: data/processed/knowledge_graph.json
  page_images: data/cache/page_images
  summary_cache: data/cache/page_summaries.json
  logs: logs/runs

qwen:
  base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: qwen-plus

vlm:
  base_url: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: qwen-vl-max

embedding:
  model: hash

reranker:
  model: lexical

retrieval:
  mode: bm25
  bm25_top_k: 40
  vector_top_k: 12
  rerank_top_k: 16
  final_top_k: 6

# 疾病范围 key，注册表见 backend/app/services/disease_scope.py
disease_scope: dlbcl

graph:
  fanout: 3
  depth: 1
  reserve: 2

# 送入 VLM 的图片硬顶（选图预算）；可按延迟/质量权衡调整
max_images: 1
display_max_figures: 2
page_image_dpi: 150
enable_evidence_gating: true

crop:
  enabled: true
  # null 表示沿用 page_image_dpi
  dpi: null
  padding: 0.02
  min_area: 0.05
  prefer: auto   # auto=确定性几何优先 | vlm=VLM兜底调试 | detect=仅矢量图检测
  vlm_max_area: 0.8
  vlm_dedup_guard: true
